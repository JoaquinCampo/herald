# Future-query diagnostic result

The fixed047 diagnostic failed both gates on all64 exposed study045 evaluation
prompts. Equal-prompt within-prompt concordance was0.6706349206, below0.81.
Spearman correlation between mean oracle error and signed final task loss was
0.2767126117, below0.70. The same error under the pending B0 query correlated
0.0326014689 with task loss. No predictor was fitted and no threshold changed.

This privileged attention-output error does not recover enough task-level signal
even with the future reference queries. It is not an information upper bound;
the result cannot prove every possible attention measurement uninformative.
Following047, stop B0 mask/attention-risk variants for this NIAH action family.
No alternative aggregation, layer/head search, proxy or post-hoc model follows.
Return to task, action, timing and scientific assumptions. Confirmation unopened.

All64 records completed and retain four exact seven-digit reference mappings.
The collector uses identical full-reference post-boundary K/V on both sides,
with the native .05 mask applied only to the original B0 cache. Current-query
self K/V is included. Float32 reconstructed projected attention differed from
native bfloat16 output by at most0.00442623 relative error across the batch,
within the predeclared0.005 tolerance. Source09364027b0fdbb2aff4a74d84e45ea6d90ee4b201a7291e3667715c5a931c2b4.

The first GPU case failed before labels while serializing numpy.bool_. The
failed run is preserved at results/future-query-oracle-v1-first. A one-line
native-bool conversion made the exact input pass at first-rerun. After adding
identity, shape and tensor-finiteness guards, the same input passed again at
future-query-oracle-v1-guarded-first before the full64 run. No outcomes or
measurement definitions changed. CPU math proof is synthetic, not task replay.

Owner verified all64 prior records contain prompt token IDs, the current
manifest hash equals the prior run hash, and every complete manifest row equals
its prior. All collection checks pass. Independent final batch audit passed22 checks,
including exact prior identities, variable native mask sizes, tensor and query
shapes, persisted hashes and independent metric/gate arithmetic. The owner reran
that audit with exit0. Report: results/future-query-oracle-independent-audit.json,
SHA5ec6c2b4542a78bd37e2c16d5c2d0dd9ea50b522bcacb3fed6e858e72c48fbd6.
Both scientific gates fail while integrity passes.
The median measured teacher-forced forward time was0.03553s; this excludes
attention reduction, validation and replay, so it is not total observation cost.

Evidence: results/future-query-oracle-v1, results/future-query-oracle-summary.json.
Summary SHA6776af641005762c05599081b28c4e88e02e874500c26eba2120ff070372430c.

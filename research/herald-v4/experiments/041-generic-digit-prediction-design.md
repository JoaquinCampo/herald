# One prospective generic numeric continuation test

Study038 failed specificity and stays closed. Hypotheses in040 remain live.
Owner clarification: the earlier027 probe measured attention eviction, not
functional likelihood. Freeze a generic numeric likelihood measurement using
fresh official unchanged NIAH contexts. Do not add layout shifts simultaneously.
This replaces040's proposed generated neutral trajectory before new measurement.

Keep Qwen2.5-7B revision, B0 split-prefill, nativeKnorm removal .10, original
128token unforced horizon and signed official score_ref minus score_action.
No controller or confirmation claim. Data:48 discovery seed2026090641 and48
locked evaluation seed2026090642, official niah_multikey_1 length4096. All parent
context variants grouped, no exact prompt/context overlap with prior v4 sets.
No outcome-based sampling. Answers are used only by the final scorer.

One fixed assistant-side sandbox prefix:
'For an independent check, the next seven digits after 3.1415926535 are '
Teacher-force '8979323'. This is a fixed world-knowledge numeric continuation,
not retrieval from the task prompt. It tests generic numeric sensitivity. Seven
tokens matches the nominal numeric answer width and is fixed before outcomes.
Verify seven native single-digit tokens and exact prefix tokenization. No case
filtering by reference likelihood. No alternative cue, horizon or reduction.

From independent full and native compressed clones, process the original pending
token plus prefix plus first six digits. Retain original logical RoPE positions
and explicit physical causal masks, previously verified against sequential HF.
z is mean seven-token logp_reference minus logp_action. Save both logp vectors,
reference mean NLL, original pending-token full-vocabulary JS, log prompt length,
and overall native removed fraction. Feature order [z, reference_nll, js,
log_prompt_length, removed_fraction]. No prompt-specific answer parser.

Fit discovery-only StandardScaler plus Ridge(alpha=1, intercept=True), with no
parameter selection or clipping. Candidate uses z alone. Baselines: discovery
mean; structural [log_prompt_length, removed_fraction]; reference_NLL alone;
JS alone; all four baseline features jointly. Every scaling fit uses discovery.
Require at least6positive and6nonpositive discovery outcomes before fitting.
Freeze model file before any evaluation outcomes. Save features and predictions
before unforced reference/noop/action continuation and scoring for each case.
No negative signed losses dropped. Evaluation requires6positive/6nonpositive,
>=10%lower MSE and>=32/48strict squared-error wins versus EACH of five baselines,
and raw positive-direction z AUC>=.80. Primary MSE, descriptive MAE/bias and
paired-prompt bootstrap95%gain intervals over frozen predictions (10000 draws,
seed2026090643), explicitly not model-refitting uncertainty. No subgroup rescue.

CPU acceptance: actual tiny Qwen paired generation, immutable independent caches,
exact noop IDs/termination, same native feature/action masks and physical effect,
finite probabilities, explicit physical causal batch versus sequential logits
at1e-5. First real GPU case must reproduce prior collector's exact reference and
action unforced IDs/termination/masks; that prior outcome is only a replay audit.
Save provenance and hashes. Record complete observation wall time from prepared
B0, plus clone/compression/forward/transfer/prediction breakdown; prefill and
correctness audit cost separately. No zero-cost or deployment claims.

If any gate fails, close this fixed generic probe and stop functional-probe
variants in this task/action population. Pivot to a more graded task/action
population rather than tuning this cue or its reduction. A pass warrants a
separately frozen genuinely unseen confirmation; none is opened in041.

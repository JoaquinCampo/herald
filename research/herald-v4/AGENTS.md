# HERALD v4

Read FRAMEWORK.md, RESEARCH.md and STATE.md first. Follow targeted links in
EVIDENCE.md when needed. V3 is a read-only evidence archive, not a default
context source. Do not import its owner-state history or copy its whole tree.

The current owner has autonomous research authority within RESEARCH.md.
Keep final scientific judgment with the owner. Delegate bounded independent
units with explicit file ownership and independent review where useful.
Workers must not overwrite each other's work or mutate historical studies.

Default to GPT-5.6 Luna for subagents, including research, implementation and
routine independent review. Use Sol, Terra or Astra only when genuine task
difficulty, unresolved uncertainty or failed attempts justify escalation.
Do not routinely invoke an expensive advisor merely to satisfy a workflow.

Optimize for fast scientific learning. Code must be clear and trustworthy for
the experiment, not production-perfect. Prioritize checks that can change the
conclusion: state isolation, intervention semantics, scoring, leakage and real
reproduction. Do not spend cycles on exhaustive tests, cosmetic refactors or
perfect types without scientific value. If tooling dominates, simplify the
experiment or return to strategy instead of endlessly polishing infrastructure.

STATE.md is current state, not a journal: rewrite it, never prepend history.
Hard cap 100 lines, target 50. Store experiment-specific records under
experiments/ and link only active or decisive evidence. Keep documentation
small and purposeful. No speculative code or scaffolding.

Use mature libraries, uv, Ruff, MyPy and pytest as needed. Validate real
behavior, not merely types or mocks. Preserve the exact failure before a fix,
distinguish competing explanations, then rerun the original failure first.
Use fail-fast verification. Record provenance, seeds and intervention semantics.
Never silently exclude failures or unscorable outcomes.

No GPU work on this Mac. Verify Orion host, separate v4 path, hardware and
process ownership before remote work. Preserve unrelated processes and the
preexisting v2 keepalive. No destructive sync, shared dependency modification,
paid services, publishing, credential changes or hardware/admin changes.

Copy reusable code only for a selected experiment, recording source path/hash
and checking its semantics. No datasets, weights, outputs or virtualenvs in Git.
Keep signed losses, prompt-grouped splits, training-only transforms, matched
information baselines and a sealed genuinely unseen confirmation population.
Changing folders never resets data exposure. Do not tune against v3 test data.

Write concise English, no em dashes. Report what evidence proves and its limits.

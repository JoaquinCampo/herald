# Complete exposed NIAH rescue diagnostic

Strategic retreat after EA, fixed-prefix and immediate-JS failures. Four
competing explanations remain: observations precede answer-bearing behavior;
global scalar disturbance discards the identity of needed content; benchmark
quality metrics respond differently; small-sample linear estimates are unstable.
The immediate-JS failure is scoped to021, not all distributional predictors.
All observations used the last-prompt boundary, not a token32 boundary.

Choose the smallest extension that checks selection bias in the existing causal
claim. Complete the fixed .10 oracle sentence-rescue versus matched non-needle
control on all12 existing exposed NIAH prompts. Reuse the four already measured
cases000/001/004/011 and run the remaining eight only. No code changes, head,
window, action, threshold or victim-selection search. Reuse exact frozen source
ace1754ac20955b5e292c76cb557c5abdee388bfb64810a32002131af77ede2a,
model, seed, shared boundary, cap and official score from011/012.

The original four-case study remains closed and unchanged; this is a separate
exploratory extension, not confirmation. Include every case regardless of its
prior outcome. Validate the same28 controls per new case and exact prior
reference/standard replay. Report all reference/standard/oracle/control scores,
paired oracle-minus-control score differences, harmed cases and ties. Any
failure is retained and blocks interpreting that case rather than being dropped.

A positive paired mean without added harms supports a content-specific rescue
mechanism across this exposed subset and motivates one answer-free proxy design.
Mixed results require examining specific failures; no rescue or tied control
weakens the mechanism. Neither outcome validates a predictor or rules out other
mechanisms. Oracle spans cannot enter deployed predictor features.

Primary-source mechanism context, transfer is inference: SnapKV
https://arxiv.org/abs/2404.14469 (prompt observation windows), H2O
https://arxiv.org/abs/2306.14048 (heavy hitters), Retrieval Heads
https://arxiv.org/abs/2404.15574 (head-specific retrieval roles). These do not
establish prediction of signed final quality loss in our protocol.
